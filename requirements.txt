# Dependências de runtime (app Gradio + Hugging Face Space), versões fixadas.
# Stack único: o MESMO requirements roda local (venv Python 3.11) e no HF Space (Python 3.11).
# Requer Python >= 3.10 (gradio 5.x). huggingface_hub é resolvido pelo próprio gradio.
gradio==5.50.0
joblib==1.5.3
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
